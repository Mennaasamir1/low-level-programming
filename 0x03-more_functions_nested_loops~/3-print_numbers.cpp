#include "main.h"
/**
 * print_numbers - prints numbers from 0 to 9 followed by newline.
 */
void print_numbers(void)
{
    unsigned short i;

    for (i = 0; i < 10; i++)
    {
        cout << i;
    }
    cout << "\n";
}